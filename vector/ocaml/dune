(executable
 (name vector)
 (ocamlopt_flags
  (:standard
   -O3
   -inline
   100
   -nodynlink
   -ccopt
   -s
   -ccopt
   -O3
   -ccopt
   -march=native)))

(rule
 (alias default)
 (target vector)
 (action
  (copy vector.exe vector))
 (mode
  (promote (until-clean))))
