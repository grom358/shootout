(executable
 (name nbody)
 (ocamlopt_flags
  (:standard
   -O3
   -inline
   100
   -nodynlink
   -ccopt
   -s
   -ccopt
   -O3
   -ccopt
   -march=native)))

(rule
 (alias default)
 (target nbody)
 (action
  (copy nbody.exe nbody))
 (mode
  (promote (until-clean))))
